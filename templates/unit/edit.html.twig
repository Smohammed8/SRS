{% extends 'base.html.twig' %}

{% block title %}Edit Unit{% endblock %}

{% block body %}
    <h3>Edit unit in  <a href="#"> <u>  {{ ward }} </u></a> </h3>

    {{ include('unit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('unit_index',{id:ward.id}) }}">Back to list</a>

    {{ include('unit/_delete_form.html.twig') }}
{% endblock %}
