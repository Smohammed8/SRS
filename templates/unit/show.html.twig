{% extends 'base.html.twig' %}

{% block title %}Unit{% endblock %}

{% block body %}
    <h1>Unit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unit.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ unit.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unit_new',{id:ward.id}) }}">Back to list</a>




    {{ include('unit/_delete_form.html.twig') }}
{% endblock %}
