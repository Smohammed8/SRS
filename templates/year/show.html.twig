{% extends 'base.html.twig' %}

{% block title %}Year{% endblock %}

{% block body %}
    <h1>Year</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ year.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_year_index') }}">back to list</a>

    <a href="{{ path('app_year_edit', {'id': year.id}) }}">edit</a>

    {{ include('year/_delete_form.html.twig') }}
{% endblock %}
