<form method="post" action="{{ path('woreda_delete', {'id': woreda.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ woreda.id) }}">
    <button class="btn">Delete</button>
</form>
