<form method="post" action="{{ path('admimssion_delete', {'id': admimssion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admimssion.id) }}">
    <button class="btn">Delete</button>
</form>
