{% extends 'base.html.twig' %}

{% block title %}Preparatory{% endblock %}

{% block body %}
    <h1>Preparatory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ preparatory.id }}</td>
            </tr>
            <tr>
                <th>StartYear</th>
                <td>{{ preparatory.startYear }}</td>
            </tr>
            <tr>
                <th>EndYear</th>
                <td>{{ preparatory.endYear }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ preparatory.result }}</td>
            </tr>
            <tr>
                <th>SchoolName</th>
                <td>{{ preparatory.schoolName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_preparatory_index') }}">back to list</a>

    <a href="{{ path('app_preparatory_edit', {'id': preparatory.id}) }}">edit</a>

    {{ include('preparatory/_delete_form.html.twig') }}
{% endblock %}
