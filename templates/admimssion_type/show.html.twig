{% extends 'base.html.twig' %}

{% block title %}AdmimssionType{% endblock %}

{% block body %}
    <h1>AdmimssionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admimssion_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ admimssion_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ admimssion_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admimssion_type_index') }}">back to list</a>

    <a href="{{ path('admimssion_type_edit', {'id': admimssion_type.id}) }}">edit</a>

    {{ include('admimssion_type/_delete_form.html.twig') }}
{% endblock %}
