{% extends 'base.html.twig' %}

{% block title %}ExamType{% endblock %}

{% block body %}
    <h1>ExamType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exam_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exam_type.name }}</td>
            </tr>
            <tr>
                <th>Min</th>
                <td>{{ exam_type.min }}</td>
            </tr>
            <tr>
                <th>Max</th>
                <td>{{ exam_type.max }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exam_type_index') }}">back to list</a>

    <a href="{{ path('app_exam_type_edit', {'id': exam_type.id}) }}">edit</a>

    {{ include('exam_type/_delete_form.html.twig') }}
{% endblock %}
