{% extends 'base.html.twig' %}

{% block title %}Ward{% endblock %}

{% block body %}
    <h1>Ward</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ward.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ward.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ward.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ward_index') }}">back to list</a>

    <a href="{{ path('ward_edit', {'id': ward.id}) }}">edit</a>

    {{ include('ward/_delete_form.html.twig') }}
{% endblock %}
