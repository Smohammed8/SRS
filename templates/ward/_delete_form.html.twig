<form method="post" action="{{ path('ward_delete', {'id': ward.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ward.id) }}">
    <button class="btn">Delete</button>
</form>
