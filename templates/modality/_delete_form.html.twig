<form method="post" action="{{ path('app_modality_delete', {'id': modality.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modality.id) }}">
    <button class="btn">Delete</button>
</form>
