{% extends 'base.html.twig' %}

{% block title %}DiseaseCategory{% endblock %}

{% block body %}
    <h1>DiseaseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disease_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ disease_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ disease_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disease_category_index') }}">back to list</a>

    <a href="{{ path('app_disease_category_edit', {'id': disease_category.id}) }}">edit</a>

    {{ include('disease_category/_delete_form.html.twig') }}
{% endblock %}
