<form method="post" action="{{ path('app_grading_delete', {'id': grading.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grading.id) }}">
    <button class="btn">Delete</button>
</form>
