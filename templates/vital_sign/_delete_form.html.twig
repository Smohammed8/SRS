<form method="post" action="{{ path('app_vital_sign_delete', {'id': vital_sign.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vital_sign.id) }}">
    <button class="btn">Delete</button>
</form>
