{% extends 'base.html.twig' %}

{% block title %}Kebele{% endblock %}

{% block body %}
    <h1>Kebele</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kebele.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kebele.name }}</td>
            </tr>
            <tr>
                <th>Discription</th>
                <td>{{ kebele.discription }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kebele_index') }}">back to list</a>

    <a href="{{ path('kebele_edit', {'id': kebele.id}) }}">edit</a>

    {{ include('kebele/_delete_form.html.twig') }}
{% endblock %}
