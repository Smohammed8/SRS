<form method="post" action="{{ path('kebele_delete', {'id': kebele.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kebele.id) }}">
    <button class="btn">Delete</button>
</form>
