<form method="post" action="{{ path('app_reason_of_appointment_delete', {'id': reason_of_appointment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reason_of_appointment.id) }}">
    <button class="btn">Delete</button>
</form>
