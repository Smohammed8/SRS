{% extends 'base.html.twig' %}

{% block title %}EncounterType{% endblock %}

{% block body %}
    <h1>EncounterType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ encounter_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ encounter_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ encounter_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_encounter_type_index') }}">back to list</a>

    <a href="{{ path('app_encounter_type_edit', {'id': encounter_type.id}) }}">Edit</a>

    {{ include('encounter_type/_delete_form.html.twig') }}
{% endblock %}
